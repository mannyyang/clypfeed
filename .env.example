# Gmail IMAP (requires App Password — see README)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-16-char-app-password
IMAP_HOST=imap.gmail.com
IMAP_PORT=993

# Anthropic (optional — omit if using Claude subscription via Claude Code login)
# ANTHROPIC_API_KEY=sk-ant-...
